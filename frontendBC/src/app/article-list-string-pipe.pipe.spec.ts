import { ArticleListStringPipePipe } from './article-list-string-pipe.pipe';

describe('ArticleListStringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleListStringPipePipe();
    expect(pipe).toBeTruthy();
  });
});
